version: '3'
services:
  web_devmoi:
    build: .
    depends_on:
      - mongoDevmoi
    command: bash -c "npm i && npm start dev"
    volumes:
      - .:/usr/app
      - /usr/app/node_modules
    working_dir: /usr/app
    ports:
      - '3000:3000'
  mongoDevmoi:
    container_name: mongoDevmoi
    image: mongo
    ports:
      - '27017:27017'
  nginx:
    build: ./nginx
    depends_on:
      - web_devmoi
    ports:
      - '80:80'
      - '443:443'
  openssl:
    build: ./openssl
